{
  "version": "2.0.0",
  "tasks": [
    {
      // Installs the locked dependencies into .venv before the agent starts.
      "label": "sync dependencies",
      "type": "shell",
      "command": "uv sync",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [],
      "presentation": {
        "reveal": "silent",
        "panel": "shared"
      }
    }
  ]
}
