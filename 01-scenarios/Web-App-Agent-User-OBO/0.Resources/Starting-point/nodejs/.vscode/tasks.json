{
  "version": "2.0.0",
  "tasks": [
    {
      // Installs the locked dependencies into node_modules before the agent starts.
      "label": "install dependencies",
      "type": "shell",
      "command": "npm install",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [],
      "presentation": {
        "reveal": "silent",
        "panel": "shared"
      }
    }
  ]
}
