{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "AzureOpenAI": {
    "//ApiKey": "Optional. Entra credentials are used when no key is set, which is recommended and is the only option where keys are disabled by policy. To use key auth do NOT add the key here - this file is committed. Use: dotnet user-secrets set \"AzureOpenAI:ApiKey\" \"<key>\", or the AzureOpenAI__ApiKey environment variable.",
    "Endpoint": "https://<your-resource>.openai.azure.com/",
    "Deployment": "gpt-4.1",
    "TenantId": "<your-tenant-id>",
    "//": "Set UseManagedIdentity to true only when the agent runs on Azure infrastructure. Off Azure there is no IMDS endpoint and managed identity fails fatally instead of falling through to the az CLI credential.",
    "UseManagedIdentity": false
  },

  "LearnMcp": {
    "Endpoint": "https://learn.microsoft.com/api/mcp"
  },

  "//": "Anonymous mode by default, so the agent runs in the Microsoft 365 Agents Playground with no setup. To connect it to Teams / Microsoft 365 Copilot, fill in the Azure Bot registration values below and keep the secret in user-secrets, never in this file.",

  "TokenValidation": {
    "Audiences": []
  },

  "Connections": {
    "ServiceConnection": {
      "Settings": {
        "AuthType": "ClientSecret",
        "AuthorityEndpoint": "https://login.microsoftonline.com/botframework.com",
        "ClientId": "",
        "ClientSecret": "",
        "Scopes": [
          "https://api.botframework.com/.default"
        ]
      }
    }
  },

  "ConnectionsMap": [
    {
      "ServiceUrl": "*",
      "Connection": "ServiceConnection"
    }
  ]
}
